import { TimePlayedPipe } from './time-played.pipe';

describe('TimePlayedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePlayedPipe();
    expect(pipe).toBeTruthy();
  });
});
