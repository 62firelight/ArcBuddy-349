import { CamelConverterPipe } from './camel-converter.pipe';

describe('CamelConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CamelConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
