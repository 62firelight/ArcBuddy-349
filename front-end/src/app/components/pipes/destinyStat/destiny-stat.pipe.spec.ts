import { DestinyStatPipe } from './destiny-stat.pipe';

describe('DestinyStatPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinyStatPipe();
    expect(pipe).toBeTruthy();
  });
});
